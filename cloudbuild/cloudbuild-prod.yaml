steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'us-central1-docker.pkg.dev/PROJECT_ID/my-app-repo/my-app-prod:$SHORT_SHA', '.']
    dir: 'app/'

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'us-central1-docker.pkg.dev/PROJECT_ID/my-app-repo/my-app-prod:$SHORT_SHA']

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      [
        'run',
        'deploy',
        'my-app-prod',
        '--image=us-central1-docker.pkg.dev/PROJECT_ID/my-app-repo/my-app-prod:$SHORT_SHA',
        '--region=us-central1',
        '--platform=managed',
        '--set-env-vars=ENV=prod',
        '--allow-unauthenticated'
      ]

options:
  logging: CLOUD_LOGGING_ONLY
